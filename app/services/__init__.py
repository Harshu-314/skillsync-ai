"""
app/services package.

Business logic layer. Routes call into services; services call into
repositories and AI modules. Empty in Sprint 0 by design — no
business logic is implemented yet. Populated starting Sprint 1
(auth_service.py) and onward, one service per feature area.
"""
