"""
app/ai package.

NLP and matching logic: spaCy-based skill extraction. Sentence
Transformer embeddings and cosine similarity are reserved for a later
sprint (skill gap comparison between resume and job-demand skills) —
this sprint's extraction problem is exact-phrase matching against a
known taxonomy, not semantic similarity.

Populated starting Sprint 3 (skill_extractor.py).
"""
