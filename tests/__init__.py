"""
tests package.

Automated test suite, run against create_app("testing") (in-memory
SQLite, per app/config.py::TestingConfig). Empty in Sprint 0 by
design — no business logic exists yet to test. Populated starting
Sprint 1 alongside the first real feature.
"""
